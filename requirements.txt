# Direct dependencies. For a fully pinned, reproducible environment see
# requirements-lock.txt (pip freeze). For CUDA/Vast.ai see requirements_cuda.txt.

# --- Core: model + LoRA fine-tuning ---
torch==2.8.0
transformers==4.56.2
peft==0.17.1
accelerate==1.10.1
datasets==4.1.1
sentencepiece==0.2.1
bitsandbytes==0.42.0        # 4-bit quantization (CUDA only)

# --- Evaluation metrics ---
sacrebleu==2.5.1
rouge-score==0.1.2
evaluate==0.4.6
scikit-learn                # cohen_kappa / MAE / MSE for the judge model

# --- Data / numerics ---
numpy==2.3.3
pandas==2.3.2
scipy==1.16.3

# --- Judge data generation ---
groq==0.34.1                # LLM-as-judge score generation

# --- Experiment tracking ---
wandb==0.23.0
weave==0.52.16

# --- Utilities ---
tqdm==4.67.1
